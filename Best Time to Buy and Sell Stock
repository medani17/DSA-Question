// Input: prices = [7,1,5,3,6,4]
// Output: 5

code:
 int maxProfit(vector<int>& prices) {
 int price = INT_MAX;
  int profit = 0;
  
  for(int i=0; i<prices.size(); i++){
   price = min(price , prices[i]);
  profit = max(profir , prices[i]-price);
  }
  return profit;
}
